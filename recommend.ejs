<%- include('layout') %>

<div class="row justify-content-center mt-4">
    <div class="col-md-10 text-center">
        <h2 class="fw-bold text-white mb-3">✨ Senin İçin Seçtiklerimiz</h2>
        <p class="text-white opacity-75 mb-5">
            Kütüphanendeki kitaplara göre bunları da sevebilirsin.
        </p>

        <div class="row">
            <% if (suggestions.length === 0) { %>
                <div class="col-12">
                    <div class="alert alert-light opacity-75">
                        Şu an önerilecek yeni bir kitap bulamadık.
                    </div>
                </div>
            <% } else { %>
                <% suggestions.forEach(book => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 border-0 shadow-lg">
                            <div class="card-body p-4 d-flex flex-column">
                                <span class="badge bg-warning text-dark align-self-center mb-3"><%= book.type %></span>
                                <h4 class="card-title fw-bold text-primary"><%= book.title %></h4>
                                <p class="card-text text-muted"><%= book.author %></p>
                                <div class="mt-auto">
                                    <form action="/add-book" method="POST">
                                        <input type="hidden" name="title" value="<%= book.title %>">
                                        <input type="hidden" name="author" value="<%= book.author %>">
                                        <button type="submit" class="btn btn-success w-100">+ Ekle</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>

        <div class="mt-4 pb-5">
            <a href="/recommend" class="btn btn-outline-light px-4 me-2">🔄 Yenile</a>
            <a href="/books" class="btn btn-link text-white text-decoration-none">Geri Dön</a>
        </div>
    </div>
</div>

</div> </body> </html>