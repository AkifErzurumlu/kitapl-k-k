<%- include('layout') %>

<div class="row justify-content-center mt-4">
    <div class="col-md-6">
        <div class="card p-4 shadow-lg border-0">
            <h3 class="mb-4 text-center fw-bold" style="color: #667eea;">Kitabı Düzenle</h3>
            
            <form action="/edit/<%= book._id %>" method="POST">
                
                <div class="mb-3">
                    <label class="form-label fw-bold">Kitap Adı</label>
                    <input type="text" name="title" class="form-control form-control-lg" value="<%= book.title %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Yazar Adı</label>
                    <input type="text" name="author" class="form-control form-control-lg" value="<%= book.author %>" required>
                </div>
                <div class="mb-3">
    <label class="form-label fw-bold">Kitap İçeriği</label>
    <div class="mb-3">
    <label class="form-label fw-bold">Kitap İçeriği</label>
    <textarea name="content" class="form-control" rows="10"><%= book.content %></textarea>
    
    <div class="form-text text-danger">
        ⚠️ <strong>Dikkat:</strong> İçeriği sadece Admin genel olarak değiştirebilir. 
        Admin değilseniz, yaptığınız değişiklikler sadece sizin profilinizde görünür, diğer kullanıcıları etkilemez.
    </div>
</div>
</div>

                <button type="submit" class="btn btn-warning btn-lg w-100 mt-3 text-white shadow-sm">
                    💾 Değişiklikleri Kaydet
                </button>
                <a href="/list" class="btn btn-outline-secondary w-100 mt-2 border-0">Vazgeç</a>
            </form>

        </div>
    </div>
</div>

</div> </body> </html>