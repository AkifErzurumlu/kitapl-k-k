<%- include('layout') %>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<div class="container mt-4">
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold" style="color: #2d3748;">Kütüphanem</h2>
            <p class="text-muted">Arşivindeki tüm kitaplar burada listeleniyor.</p>
        </div>
        <a href="/add" class="btn btn-primary px-4 py-2 fw-bold shadow-sm" style="background-color: #667eea; border: none; border-radius: 30px;">
            + Yeni Kitap Ekle
        </a>
    </div>

    <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
        
        <% if (books.length === 0) { %>
            <div class="col-12 text-center mt-5">
                <div class="alert alert-light py-5 shadow-sm">
                    <h4>Henüz hiç kitabın yok! 📚</h4>
                    <p class="text-muted">Hemen yukarıdaki butona basıp ilk kitabını ekle.</p>
                </div>
            </div>
        <% } else { %>
            
            <% books.forEach(book => { %>
                <div class="col">
                    <div class="card h-100 border-0 shadow-sm hover-card" style="transition: transform 0.2s; border-radius: 15px; overflow: hidden;">
                        
                        <div style="height: 280px; overflow: hidden; position: relative; background-color: #f8f9fa;">
                            
                            <% if (book.isRead) { %>
                                <span class="badge bg-success position-absolute top-0 end-0 m-3 shadow">Okundu</span>
                            <% } %>

                            <img src="<%= book.imageUrl ? book.imageUrl : 'https://images.unsplash.com/photo-1543002588-bfa74002ed7e?auto=format&fit=crop&w=500&q=60' %>" 
                                 class="card-img-top" 
                                 alt="<%= book.title %>" 
                                 style="width: 100%; height: 100%; object-fit: cover;">
                        </div>

                        <div class="card-body text-center p-3">
                            <h6 class="card-title fw-bold text-truncate" title="<%= book.title %>"><%= book.title %></h6>
                            <p class="card-text text-muted small mb-3"><%= book.author %></p>
                            
                            <a href="/read/<%= book._id %>" class="btn btn-primary w-100 mb-3 shadow-sm" style="background-color: #667eea; border: none; border-radius: 10px;">
                                İncele & Oku
                            </a>
                            
                            <div class="d-flex justify-content-center gap-2">
                                
                                <form action="/mark-read/<%= book._id %>" method="POST">
                                    <button type="submit" class="btn btn-light rounded-circle shadow-sm text-success" style="width: 40px; height: 40px;" title="<%= book.isRead ? 'Okunmadı Yap' : 'Okundu Yap' %>">
                                        <i class="fas fa-check"></i>
                                    </button>
                                </form>
                                
                                <a href="/edit/<%= book._id %>" class="btn btn-light rounded-circle shadow-sm text-warning" style="width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;" title="Düzenle">
                                    <i class="fas fa-edit"></i>
                                </a>
                                
                                <form action="/delete/<%= book._id %>" method="POST" onsubmit="return confirm('Bu kitabı silmek istediğine emin misin?');">
                                    <button type="submit" class="btn btn-light rounded-circle shadow-sm text-danger" style="width: 40px; height: 40px;" title="Sil">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>

                            </div>

                        </div>

                    </div>
                </div>
            <% }) %>

        <% } %>
    </div>
</div>

<style>
    .hover-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
</style>

</div> </body> </html>