<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Akif'in Kütüphanesi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* MAVİ HERO TASARIMI (Senin Sevdiğin) */
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 100px 0;
            margin-bottom: 50px;
        }
        .hero-card {
            background: rgba(255, 255, 255, 0.95);
            color: #333;
            border-radius: 15px;
            padding: 3rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        .book-card {
            transition: transform 0.2s;
            border: none;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .book-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/books">📚 Kütüphanem</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/books">Ana Sayfa</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="#" data-bs-toggle="modal" data-bs-target="#addBookModal">Kitap Ekle</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#kitap-listesi">Kitap Listesi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" onclick="alert('Geliştirici: Emin Akif Erzurumlu')">Hakkımda</a>
                    </li>
                    <li class="nav-item ms-3">
                        <a href="/logout" class="btn btn-outline-danger btn-sm mt-1">Çıkış Yap</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="hero-section text-center">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 hero-card">
                    <h1 class="display-4 fw-bold mb-3">Hoş Geldin!</h1>
                    <p class="fs-4 text-muted">
                        Şu an kütüphanende tam <span class="badge bg-primary rounded-pill"><%= totalBooks %></span> kitap bulunuyor.
                    </p>
                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-4">
                        <button type="button" class="btn btn-primary btn-lg px-4 gap-3" data-bs-toggle="modal" data-bs-target="#addBookModal">
                            + Yeni Kitap Ekle
                        </button>
                        <a href="#kitap-listesi" class="btn btn-outline-secondary btn-lg px-4">Listeyi Gör</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="kitap-listesi" class="container pb-5">
        <h2 class="text-center mb-4 fw-bold text-secondary">📖 Kitapların</h2>
        
        <div class="row">
            <% if (books.length === 0) { %>
                <div class="col-12 text-center">
                    <div class="alert alert-warning">
                        Henüz hiç kitap eklemedin. Yukarıdaki butona bas ve ilk kitabını ekle! 🚀
                    </div>
                </div>
            <% } else { %>
                <% books.forEach(book => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card book-card h-100">
                            <div class="card-body">
                                <h5 class="card-title fw-bold text-primary"><%= book.title %></h5>
                                <h6 class="card-subtitle mb-2 text-muted"><%= book.author %></h6>
                                <p class="card-text text-secondary small">Eklenme Tarihi: Şuan</p>
                            </div>
                            <div class="card-footer bg-white border-0">
                                <form action="/delete-book/<%= book._id %>" method="POST">
                                    <button type="submit" class="btn btn-outline-danger w-100 btn-sm">Sil</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>

    <div class="modal fade" id="addBookModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">Yeni Kitap Ekle</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form action="/add-book" method="POST">
                        <div class="mb-3">
                            <label class="form-label">Kitap Adı</label>
                            <input type="text" name="title" class="form-control" placeholder="Örn: Sefiller" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Yazar Adı</label>
                            <input type="text" name="author" class="form-control" placeholder="Örn: Victor Hugo" required>
                        </div>
                        <button type="submit" class="btn btn-success w-100">Kaydet ve Bitir</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>