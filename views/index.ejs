<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kütüphanem</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container-fluid py-5 text-center bg-white shadow-sm mb-4">
        <h1 class="display-5 fw-bold">Hoş Geldin!</h1>
        <p class="fs-4">Şu an kütüphanende tam <strong><%= totalBooks %></strong> kitap bulunuyor.</p>
        <a href="#kitap-ekle" class="btn btn-primary btn-lg">Yeni Kitap Ekle</a>
    </div>

    <div class="container">
        <% if (totalBooks === 0) { %>
            <div class="alert alert-warning text-center">
                Henüz hiç kitap eklemedin. Hadi başlayalım!
            </div>
        <% } %>

        <div class="row">
            
            <div class="col-md-4 mb-4" id="kitap-ekle">
                <div class="card shadow-sm sticky-top" style="top: 20px;">
                    <div class="card-header bg-dark text-white">
                        <h5 class="mb-0">Hızlı Ekle</h5>
                    </div>
                    <div class="card-body">
                        <form action="/add-book" method="POST">
                            <div class="mb-3">
                                <label class="form-label">Kitap Adı</label>
                                <input type="text" name="title" class="form-control" required>
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Yazar</label>
                                <input type="text" name="author" class="form-control" required>
                            </div>
                            <button type="submit" class="btn btn-success w-100">Kaydet</button>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="list-group shadow-sm">
                    <% books.forEach(book => { %>
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <h5 class="mb-1"><%= book.title %></h5>
                                <small class="text-muted"><%= book.author %></small>
                            </div>
                            <form action="/delete-book/<%= book._id %>" method="POST" class="m-0">
                                <button type="submit" class="btn btn-outline-danger btn-sm">Sil</button>
                            </form>
                        </div>
                    <% }) %>
                </div>
            </div>

        </div>
    </div>

</body>
</html>