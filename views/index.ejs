<%- include('layout') %>

<div class="container mt-4">
    
    <div class="row mb-5">
        <div class="col-12">
            <div class="glass-box text-center">
                <h1 class="display-4 fw-bold" style="color: #4a5568;">
                    Hoşgeldin, <%= user.username.charAt(0).toUpperCase() + user.username.slice(1) %>! 👋
                </h1>
                <p class="lead text-muted">Kütüphanen seni bekliyor. Bugün ne okumak istersin?</p>
                
                <a href="/add" class="btn btn-primary btn-lg fw-bold mt-3 px-5 shadow-sm" style="border-radius: 30px; background-color: #667eea; border: none;">
                    + Yeni Kitap Ekle
                </a>
            </div>
        </div>
    </div>

    <div class="row g-4 text-center">
        
        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100 py-3">
                <div class="card-body">
                    <i class="fas fa-book fa-3x mb-3" style="color: #667eea;"></i>
                    <h5 class="text-muted text-uppercase small fw-bold">Toplam Kitap</h5>
                    <h2 class="display-5 fw-bold text-dark"><%= user.books.length %></h2>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100 py-3">
                <div class="card-body">
                    <i class="fas fa-check-circle fa-3x mb-3 text-success"></i>
                    <h5 class="text-muted text-uppercase small fw-bold">Bitirilenler</h5>
                    <h2 class="display-5 fw-bold text-dark">
                        <%= user.books.filter(book => book.isRead).length %>
                    </h2>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100 py-3">
                <div class="card-body">
                    <i class="fas fa-hourglass-half fa-3x mb-3 text-warning"></i>
                    <h5 class="text-muted text-uppercase small fw-bold">Okunacaklar</h5>
                    <h2 class="display-5 fw-bold text-dark">
                        <%= user.books.filter(book => !book.isRead).length %>
                    </h2>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-12 text-center">
            <a href="/list" class="btn btn-outline-light btn-lg px-5 shadow-sm" style="border-radius: 50px; background-color: rgba(255,255,255,0.2); border: 1px solid white;">
                📚 Tüm Kitaplarımı Gör
            </a>
        </div>
    </div>

</div>

</div> </body> </html>