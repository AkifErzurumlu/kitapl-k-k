<style>
        body {
            font-family: 'Poppins', sans-serif;
            
            /* ESKİ MOR RENGİ SİLDİK, YERİNE RESİM GELDİ 👇 */
            background: url('https://images.unsplash.com/photo-1507842217121-9eac59e749e5?auto=format&fit=crop&w=1920&q=80') no-repeat center center fixed;
            background-size: cover;
            
            /* Yazılar okunsun diye resmin üzerine siyah perde çektik */
            box-shadow: inset 0 0 0 2000px rgba(0, 0, 0, 0.5); 
            
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            color: white; /* Sayfa genelindeki yazıları beyaz yaptık */
        }

        /* Başlıklar daha net görünsün */
        h1, h2, h3, h4, h5, h6, p {
            color: white !important;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8); /* Yazıya gölge */
        }

        .navbar {
            background-color: rgba(255, 255, 255, 0.95) !important;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .navbar-brand {
            font-weight: 600;
            color:rgba(0, 0, 0, 0.8) !important;
        }
        .nav-link {
            color: #333 !important; /* Menü yazıları koyu kalsın */
            font-weight: 500;
        }
        .nav-link:hover {
            color: #667eea !important;
        }
        .container {
            flex: 1;
            margin-top: 30px;
        }
        
        /* Kartların içi beyaz kalsın ki kitaplar net görünsün */
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
            background-color: rgba(255, 255, 255, 0.95); /* Hafif saydam beyaz */
            backdrop-filter: blur(5px);
            color: #333; /* Kart içindeki yazılar siyah olsun */
        }

        /* Kart içindeki başlıklar siyah olsun (Genel beyaz kuralını eziyoruz) */
        .card h1, .card h2, .card h3, .card h4, .card h5, .card h6, .card p {
            color: #333 !important;
            text-shadow: none !important;
        }

        footer {
            margin-top: auto;
            background-color: rgba(0, 0, 0, 0.6); /* Footer biraz daha koyu */
            color: white;
            text-align: center;
            padding: 15px 0;
            font-size: 0.9rem;
        }

        .navbar-toggler {
            border: 2px solid #667eea;
            outline: none;
        }
        
        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba(102, 126, 234, 1)' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e") !important;
        }
    </style>